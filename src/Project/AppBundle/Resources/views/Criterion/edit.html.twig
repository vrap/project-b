{% extends 'ProjectAppBundle::layout.html.twig' %}

{% block main -%}
    <h1>Criterion edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('criterion') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
